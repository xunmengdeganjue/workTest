--- a/src/connections.c	2010-08-17 17:04:38.000000000 +0800
+++ b/src/connections.c	2013-03-08 09:31:00.715240100 +0800
@@ -682,6 +682,7 @@
 	CLEAN(physical.basedir);
 	CLEAN(physical.rel_path);
 	CLEAN(physical.etag);
+	CLEAN(physical.key); //djnakata
 	CLEAN(parse_request);
 
 	CLEAN(authed_user);
